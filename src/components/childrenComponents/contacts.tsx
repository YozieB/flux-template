// import React, {FC, ReactNode} from "react";
// import { ContactsType } from "@/types";

// type social = ContactsType


// export const Contacts = (social) => {
//     social && 
//     return( <ul className='mt-[18px]'>
//             {social.map((social) => <li key={social.name} className='text-[8px]'>{social.link}</li>)}
//         </ul>)
// }