import './index.css';

import React, { FC } from 'react';

import { TemplateProps } from './types';

export const Flux: FC<TemplateProps> = ({
    firstName,
    lastName,
    skills,
    role,
    experience,
    languages,
    education,
    socials,
}) => {
    return (
        <main>
            Пустой шаблон
        </main>
    );
};
